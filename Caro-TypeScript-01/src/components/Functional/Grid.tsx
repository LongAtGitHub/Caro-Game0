import { Button } from "react-bootstrap";
import './Grid.css'

function Grid() {
    return (
        <div>
            <Button  className="buttonStyle" style = {{whiteSpace:'pre'}}>{' '}</Button>
        </div>
    );
}

export default Grid;